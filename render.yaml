services:
  - type: web
    name: iberian-ham-dashboard
    env: python
    region: ohio          # us-east-1, closest to ET timezone
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api_server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60
    envVars:
      # ── Shopify ──────────────────────────────────────────────
      - key: SHOPIFY_STORE
        sync: false       # set this in Render dashboard (never commit to git)
      - key: SHOPIFY_TOKEN
        sync: false
      # ── Amazon SP-API ─────────────────────────────────────────
      - key: AMAZON_CLIENT_ID
        sync: false
      - key: AMAZON_CLIENT_SECRET
        sync: false
      - key: AMAZON_REFRESH_TOKEN
        sync: false
      - key: AMAZON_MARKETPLACE_ID
        value: ATVPDKIKX0DER   # US marketplace (safe to commit)
      - key: AMAZON_REGION
        value: us-east-1
      # ── Fee config ────────────────────────────────────────────
      - key: AMAZON_FEE_PCT
        value: "15"
      - key: SHOPIFY_FEE_PCT
        value: "2"
      - key: STRIPE_FEE_PCT
        value: "2.9"
      - key: STRIPE_FIXED_FEE
        value: "0.30"
      - key: COGS_PER_UNIT
        value: "18"        # update to your actual cost per unit
      - key: SHIPPING_PER_ORDER
        value: "8"
      # ── Dashboard password ────────────────────────────────────
      - key: DASHBOARD_PASSWORD
        sync: false       # set in Render dashboard — leave blank for no password
      # ── Telegram ──────────────────────────────────────────────
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: DIGEST_SECRET
        sync: false       # set in Render dashboard — must match GitHub secret
      # ── Carrier Tracking ──────────────────────────────────────
      - key: FEDEX_CLIENT_ID
        sync: false
      - key: FEDEX_CLIENT_SECRET
        sync: false
      - key: UPS_CLIENT_ID
        sync: false
      - key: UPS_CLIENT_SECRET
        sync: false
      - key: USPS_CLIENT_ID
        sync: false
      - key: USPS_CLIENT_SECRET
        sync: false
      # ── Weekly digest: Klaviyo / GoMarble / Anthropic ─────────
      - key: KLAVIYO_API_KEY
        sync: false
      - key: GOMARBLE_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
